如何利用正则表达式，在被搜索的内容中，一旦搜索到关键字，就在关键里的两端加入标签，并保持被搜索内容中关键字的大小写和原来一致？

我用的方法是虽然可以高亮关键字，但是是用正则/keywords/gi做搜索，然后用"<span class='highlignt'>"+RegEx.$1+"</span>"替换，会导致原文中关键字的大小写改变。如，在搜索框中输入java，会使原文中的JAVA变成java。

并不想写个循环找出关键字位置，加入标签，好烦琐。感觉应该能用正则表达解决。